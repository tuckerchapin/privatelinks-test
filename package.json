{
  "name": "privatelinks-proxy",
  "version": "1.0.0",
  "description": "Minimal proxy server for VPC private links",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
